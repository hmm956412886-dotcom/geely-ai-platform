# Agent Acceptance Workspace

- Read `README.md` before changing files.
- Reuse `python project_cli.py inspect` to inspect the module contract.
- Put tests under `tests/` and run `python -m unittest discover -s tests -p "test_*.py"`.
- Do not change `calculator.py` or `project_cli.py` for the acceptance task.
