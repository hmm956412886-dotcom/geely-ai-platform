# Calculator Sample

This workspace verifies that a coding agent can discover project instructions and an existing CLI.

`calculator.py` defines integer addition. Use the project CLI to inspect its supported operation, then add a focused unit test under `tests/` and run the documented test command.
